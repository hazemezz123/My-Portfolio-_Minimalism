/**
 * @deprecated — import from "@/lib/db" and "@/lib/types" instead.
 * This re-export exists for backward compatibility.
 */
export { getDb } from "@/lib/db";
export type {
  Project,
  GuestbookEntry,
  SiteConfig,
} from "@/lib/types";
